import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'm_app_tanakit',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
